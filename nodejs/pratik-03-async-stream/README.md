# Pratik 3: Async ve stream

**Kod odaklı:** `delay`, `copyStream`, readline, `runWithLimit`, writable collect, `sequence`, `pipeline`, `eventOnce`, `withTimeout`.

**10 soru.** `README.md` (soru) + `CEVAP.md` (cevap).
