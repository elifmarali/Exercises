# Pratik 1: Modül ve fs

**Kod odaklı:** `readJson` / `writeJson`, `listFiles` recursive, `requireDir`, `exists`, `mkdirp`, `readLines`, `safeJoin`, `transformFiles`.

**10 soru.** `README.md` (soru) + `CEVAP.md` (cevap).
