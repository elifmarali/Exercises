# Asenkron JavaScript

Promise, `async`/`await`, microtask vs macrotask, `Promise.all` / `allSettled` / `race`, hata yönetimi, `fetch`, event loop sıralaması.

**20 soru.** Her soru kendi klasöründe; `README.md` (soru) + `CEVAP.md` (cevap). Hedef 100+ soru.
