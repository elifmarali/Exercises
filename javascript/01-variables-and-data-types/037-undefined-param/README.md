# Fonksiyon çağrısında eksik parametreler nasıl ele alınır? `function f(a, b) {}` için `f(1)` çağrıldığında `b` nedir?

Default parametre `function f(a, b = 2) {}` `f(1)` ve `f(1, undefined)` için nasıl davranır?