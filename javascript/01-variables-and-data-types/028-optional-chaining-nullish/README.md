# Optional chaining (`?.`) ve nullish coalescing (`??`) ne işe yarar? `obj?.a?.b` ile `obj && obj.a && obj.a.b` farkı nedir?

`??` ile `||` farkı nerede? `0`, `''` gibi falsy geçerli değerler için hangisi daha güvenli?