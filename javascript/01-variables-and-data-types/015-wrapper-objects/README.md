# “Wrapper objects” (`Number`, `String`, `Boolean`) nedir?

`typeof new Number(1)` ne döner? `'a'.toUpperCase()` gibi primitive üzerinde metot çağrısı nasıl çalışır? `new Number(1)` kullanmak neden genelde önerilmez?