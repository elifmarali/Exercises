# `Error` objeleri normal objelerden farklı mıdır? `typeof new Error()`, `instanceof Error` ne döner?

`throw` ile atılan değerler neden genelde `Error` instance’ı olmalı? `Error.prototype.message`, `stack` nasıl kullanılır?