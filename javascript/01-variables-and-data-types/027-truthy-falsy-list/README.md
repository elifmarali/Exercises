# JavaScript’te “falsy” ve “truthy” değerler nelerdir? Hepsinin listesini ver.

`if (x)` gibi kontrollerde `0`, `''`, `NaN`, `null`, `undefined` nasıl davranır? “Falsy ama geçerli değer” (örn. `0`, `''`) kullanıldığında ne tür hatalar oluşabilir ve nasıl önlenir?