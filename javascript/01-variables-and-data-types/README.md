# Değişkenler ve Veri Tipleri

`let` / `var` / `const`, scope, hoisting, TDZ, primitive vs referans, `typeof`, `null` / `undefined`, Symbol, BigInt, type conversion, optional chaining, nullish coalescing ve ilgili edge case’ler.

**100 soru.** Her soru kendi `001-xxx` … `100-xxx` klasöründe; `README.md` (soru) + `CEVAP.md` (cevap).
