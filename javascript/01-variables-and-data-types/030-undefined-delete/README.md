# `delete` ile `undefined` atama (`obj.x = undefined`) arasındaki fark nedir? `'x' in obj` ve `obj.x` açısından nasıl farklılık gösterir?

Property’nin “yok” olması ile “var ama undefined” olması `for..in`, `Object.keys`, `JSON.stringify` gibi işlemlerde nasıl yansır?