# `in` operatörü ne yapar? `'x' in obj` ile `obj.hasOwnProperty('x')` arasındaki fark nedir?

Prototip zincirindeki property'ler `in` ile nasıl görünür? `Object.create(null)` ile oluşturulan objelerde `hasOwnProperty` kullanırken neden dikkat etmek gerekir?