# IIFE (Immediately Invoked Function Expression) geleneksel olarak ne işe yarıyordu? Günümüzde hangi alternatifler kullanılır?

`(function () { var x = 1; })();` ile `{ let x = 1; }` block’u arasında scope açısından fark var mı?