# `delete` operatörü neyi siler, neyi silemez? `delete obj.x` ile `delete arr[1]` davranışı nasıldır?

`var` / `let` / `const` ile tanımlanan değişkenler, fonksiyon parametreleri ve `configurable: false` property'ler `delete` ile neden silinemez?