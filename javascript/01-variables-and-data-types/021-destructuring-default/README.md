# Destructuring ile default değer nasıl verilir? `const { x = 1 } = {}` ile `const { x = 1 } = { x: null }` sonuçları neden farklıdır?

`undefined` vs `null` (ve diğer falsy değerler) default değer tetiklemesi açısından nasıl davranır?