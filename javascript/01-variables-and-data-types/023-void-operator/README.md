# `void` operatörü ne yapar? `void 0` neden bazen `undefined` yerine kullanılır?

`void`'ın return değeri nedir? Eski kodlarda `undefined`'ın override edilebilmesi nasıl bir problem yaratırdı? Modern JS'te hâlâ kullanım alanı var mı?